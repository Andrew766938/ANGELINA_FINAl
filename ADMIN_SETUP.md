# 🛠️ SQLAdmin - Админ-панель

## Установка SQLAdmin

### 1️⃣ Установите пакеты

```bash
# Установка SQLAdmin и зависимостей
pip install -r requirements_admin.txt

# Или отдельно
pip install sqladmin==0.18.3 Werkzeug==3.0.1
```

### 2️⃣ Проверьте установку

После установки SQLAdmin уже подключен в `main.py`.

### 3️⃣ Запустите приложение

```bash
python main.py
```

Вы должны увидеть в консоли:
```
✅ SQLAdmin админ-панель подключена на /admin
```

### 4️⃣ Откройте админ-панель

Перейдите по адресу:
```
http://localhost:8000/admin
```

## 📊 Доступные модели в админ-панели

### 👤 Пользователи (Users)
- Просмотр всех пользователей
- Редактирование данных пользователя
- Удаление пользователей
- Управление ролями

### 🔒 Роли (Roles)
- Просмотр всех ролей
- Создание новых ролей
- Редактирование ролей
- Удаление ролей

### ✈️ Рейсы (Flights)
- Просмотр всех рейсов
- Создание новых рейсов
- Редактирование параметров рейса
- Управление доступными местами
- Удаление рейсов

### 🏢 Аэропорты (Airports)
- Просмотр всех аэропортов
- Создание новых аэропортов
- Редактирование информации об аэропорте
- Удаление аэропортов

### 🎫 Бронирования (Bookings)
- Просмотр всех бронирований
- Просмотр деталей бронирования
- Изменение статуса бронирования
- Удаление бронирований

## 🎨 Интерфейс

Админ-панель включает:
- ✅ Красивый интерфейс с иконками
- ✅ Поиск по записям
- ✅ Фильтрацию
- ✅ Сортировку
- ✅ Пагинацию
- ✅ Просмотр деталей
- ✅ Редактирование inline
- ✅ Удаление записей

## ⚙️ Конфигурация

Основная конфигурация находится в файле `app/admin.py`:

```python
from app.admin import setup_admin

# В main.py
setup_admin(app)
```

## 🔐 Безопасность (TODO)

В производстве добавьте:
- Аутентификацию для админ-панели
- Авторизацию (только администраторы)
- HTTPS
- Rate limiting

## 📝 Пример использования

### Просмотр рейсов
1. Откройте http://localhost:8000/admin
2. Нажмите на "Рейсы" в боковом меню
3. Все рейсы отображены в таблице

### Редактирование рейса
1. Нажмите на строку рейса
2. Измените нужные параметры
3. Нажмите "Save"

### Создание аэропорта
1. Откройте "Аэропорты"
2. Нажмите "+"
3. Заполните форму
4. Сохраните

## 🐛 Решение проблем

### Ошибка: "No module named 'sqladmin'"
```bash
pip install sqladmin
```

### Ошибка: "Не подключается к БД"
- Проверьте, что БД существует
- Проверьте путь к БД в `.env`
- Попробуйте пересоздать БД

### Админ-панель не открывается
- Проверьте, запущен ли сервер на http://localhost:8000
- Проверьте логи ошибок в консоли
- Убедитесь, что установлен SQLAdmin

## 📚 Дополнительно

- [SQLAdmin документация](https://aminalaee.dev/sqladmin/)
- [FastAPI](https://fastapi.tiangolo.com/)
- [SQLAlchemy](https://www.sqlalchemy.org/)
