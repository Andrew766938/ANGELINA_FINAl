# 🛫 Крылья онлайн - Система бронирования авиа билетов

**Современная система управления авиарейсами и бронированием билетов** на Python/FastAPI.

---

## 📋 Оглавление

- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Первый запуск](#-первый-запуск)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [Решение проблем](#-решение-проблем)

---

## 🚀 Быстрый старт

### Для нетерпеливых (30 секунд)

```bash
git clone https://github.com/Andrew766938/ANGELINA_FINAl.git
cd ANGELINA_FINAl
uv sync
uvicorn main:app --reload
```

Открой в браузере: **http://localhost:8000** ✅

---

## 🛠 Установка

### Требования

- **Python 3.10+**
- **pip** или **uv** (рекомендуется)
- **Git**

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Andrew766938/ANGELINA_FINAl.git
cd ANGELINA_FINAl
```

### Шаг 2: Создание окружения

**Вариант A: Использование uv (рекомендуется)**

```bash
uv sync
```

**Вариант B: Использование pip + venv**

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 3: Настройка переменных окружения

```bash
# Скопировать шаблон
cp .env.example .env
```

**Содержимое `.env`** (по умолчанию уже правильно):

```env
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DB_NAME=test.db
```

---

## ▶️ Первый запуск

### Запуск API

```bash
uvicorn main:app --reload
```

**Вывод (при первом запуске):**

```
╭─────────────────────────────────────────────────────────╮
╰────── 💣 Крылья онлайн стартует... 💣 ────────╯

🗄️  Проверка БД...
🔴 Таблицы не найдены. Создаю...
✅ Таблицы созданы
🌱 Загружаю тестовые данные...
✅ Загружено 5 тестовых аэропортов
✅ Приложение готово!

INFO:     Uvicorn running on http://0.0.0.0:8000
```

✅ **БД создана автоматически!** Никакие команды не нужны.

### Откройте в браузере

```
http://localhost:8000
```

---

## 💻 Использование

### Веб-интерфейс

- **Главная страница:** http://localhost:8000
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **Админ-панель:** http://localhost:8000/admin (если настроена)

### Основные операции

**Просмотр рейсов:**

```bash
curl http://localhost:8000/flights/
```

**Просмотр аэропортов:**

```bash
curl http://localhost:8000/flights/airports/
```

**Создание бронирования:**

```bash
curl -X POST http://localhost:8000/bookings/ \
  -H "Content-Type: application/json" \
  -d '{
    "flight_id": 1,
    "passenger_name": "Иван Петров",
    "passenger_email": "ivan@example.com",
    "passenger_phone": "+7-999-123-4567",
    "seats_count": 2
  }'
```

---

## 📁 Структура проекта

```
ANGELINA_FINAl/
├── 📄 main.py                      # Главное приложение
├── 📄 .env.example                 # Шаблон окружения
├── 📄 .gitignore                   # Git фильтр
├── 📄 README.md                    # Этот файл
│
├── 📁 app/
│   ├── 📁 api/                     # API маршруты
│   │   ├── flights.py              # Рейсы
│   │   ├── bookings.py             # Бронирования
│   │   └── ...
│   │
│   ├── 📁 models/
│   │   ├── flight.py               # Модель рейса
│   │   ├── booking.py              # Модель бронирования
│   │   └── __init__.py             # ✅ Очищен (без импортов)
│   │
│   ├── 📁 database/
│   │   ├── base.py                 # ✅ Base класс
│   │   ├── database.py             # ✅ Engine + Session
│   │   └── db_manager.py
│   │
│   ├── admin.py                    # SQLAdmin конфиг
│   └── config.py                   # Настройки
│
├── 📁 migrations/                  # Alembic миграции
│   ├── env.py
│   └── versions/
│
└── 📄 test.db                      # ✅ Создаётся при первом запуске
```

---

## 🔧 Команды для разработки

### Стандартные операции

```bash
# Запуск с автоперезагрузкой
uvicorn main:app --reload

# Запуск на другом порту
uvicorn main:app --reload --port 8001

# Запуск в фоне
uvicorn main:app &
```

### Работа с БД

```bash
# Проверить существование БД
ls -la test.db

# Просмотреть таблицы (SQLite)
sqlite3 test.db ".tables"

# Просмотреть данные
sqlite3 test.db "SELECT * FROM airports;"

# Полный сброс БД (удалить и пересоздать)
rm test.db
uvicorn main:app --reload
```

### Очистка кэша Python

```bash
# Windows
rd /s /q __pycache__ app/__pycache__ app/api/__pycache__ app/database/__pycache__ app/models/__pycache__

# Mac/Linux
find . -type d -name __pycache__ -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
```

---

## ❓ Решение проблем

### Ошибка: "ImportError: cannot import name 'Base'"

**Решение:**

```bash
# 1. Очистить кэш
find . -type d -name __pycache__ -exec rm -rf {} +

# 2. Удалить БД
rm test.db

# 3. Перезапустить
uvicorn main:app --reload
```

### Ошибка: "No such table: airports"

**Решение:**

```bash
# БД должна создаться при запуске
# Если не создалась:

rm test.db
uvicorn main:app --reload
```

### Port 8000 уже используется

```bash
# Использовать другой порт
uvicorn main:app --reload --port 8001
```

### Зависимости не установились

```bash
# Переустановить
# Вариант 1: uv
uv sync --force

# Вариант 2: pip
pip install --force-reinstall -r requirements.txt
```

---

## 📚 Дополнительные ресурсы

- **FastAPI документация:** https://fastapi.tiangolo.com/
- **SQLAlchemy документация:** https://docs.sqlalchemy.org/
- **Alembic миграции:** [ALEMBIC_QUICK_START.md](ALEMBIC_QUICK_START.md)
- **Полная настройка:** [FINAL_SETUP.md](FINAL_SETUP.md)

---

## ✅ Чек-лист перед использованием

- [ ] Python 3.10+ установлен
- [ ] Git репозиторий клонирован
- [ ] Зависимости установлены (`uv sync` или `pip install -r requirements.txt`)
- [ ] `.env` файл создан (из `.env.example`)
- [ ] API запущен (`uvicorn main:app --reload`)
- [ ] Браузер открыт на `http://localhost:8000`
- [ ] Кнопки работают ✅

---

## 🎯 Быстрые ссылки

| Ссылка | Описание |
|--------|----------|
| http://localhost:8000 | Веб-интерфейс |
| http://localhost:8000/docs | Swagger UI |
| http://localhost:8000/redoc | ReDoc |
| http://localhost:8000/admin | Админ-панель (если включена) |

---

## 📝 Лицензия

Этот проект создан в образовательных целях.

---

## 👨‍💻 Разработка

**Версия:** 1.0.0  
**Язык:** Python 3.10+  
**Framework:** FastAPI  
**БД:** SQLite (разработка) / PostgreSQL (production)  

---

## 💬 Поддержка

Если что-то не работает:

1. Проверьте раздел [Решение проблем](#-решение-проблем)
2. Очистите кэш и переstarт приложения
3. Убедитесь, что Python 3.10+
4. Проверьте, что `.env` скопирован из `.env.example`

---

🎉 **Готово! Приложение работает!**
