# 👤 Инструкция для друга

## Проблема

Когда ты впервые загружаешь проект, все эндпоинты выдают **Ошибка 500** до того, как база данных не была инициализирована.

## Настройка

### 1️⃣ Клонирование репозитория

```bash
git clone <url>
cd ANGELINA_FINAl
```

### 2️⃣ Установка зависимостей

```bash
# Если используется UV (pyproject.toml)
uv sync

# Или пип
# pip install -r requirements.txt
```

### 3️⃣ Копирование .env файла

```bash
# Проверь .env использует по умолчанию test.db
# Ничего не исправыть не нужно
cat .env
```

### 4️⃣ Основное: Инициализация БД

**Это самое важное!**

```bash
# Первый раз - нужно настроить БД
python setup_db.py --reset --seed

# Это сделает:
# 1. Удалит старую БД
# 2. Готовит структуру таблиц
# 3. Загружает тестовые аэропорты
```

**Опции:**
```bash
python setup_db.py              # Полностью автоматично

python setup_db.py --reset      # Цинул окончательно

python setup_db.py --seed       # Укопните тестовых аэропортов
```

### 5️⃣ Запуск проекта

```bash
# Terminal 1: руни API
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Terminal 2: открой в браузере
open http://localhost:8000
```

### 6️⃣ Тестирование АПИ

```bash
# Проверь что все работает
curl http://localhost:8000/flights/

# У тебя должны быть рейсы
```

## ✅ Проверка что работает

### Проверить таблицы

```bash
sqlite3 test.db ".tables"

# Ю должны видеть:
# airports  bookings  flights  ...
```

### Проверить данные

```bash
# Открой терминал до БД
sqlite3 test.db

# Типичные команды
sqlite> SELECT COUNT(*) FROM airports;
sqlite> SELECT * FROM airports LIMIT 5;
sqlite> .quit
```

## ⚠️ Можные проблемы

### Ошибка: "Модуль не найден"

```bash
# Проверь директорию
pwd
# Правильно: /path/to/ANGELINA_FINAl

# Нужно запускать из корня проекта
```

### Ошибка: "Таблицы не найдены"

```bash
# Перепусти setup
python setup_db.py --reset --seed

# Проверь test.db существует
ls -lh test.db
```

### Ошибка: "API не отвечает"

```bash
# Проверь что процесс работает
lsof -i :8000

# На Mac: давай порт

# Или запусти его с другим портом
uvicorn main:app --reload --port 8001
```

## 📄 Повторные запуски

Текущие прибывания не нужно ресетить - бд схраняется.

```bash
# Останови данные не трогая
uvicorn main:app --reload

# Мто если хочешь остановить всю бд и стартнуть с нуля
rm test.db
python setup_db.py --reset --seed
```

## 🎉 Профит!

Теперь ты можешь:
- 🎀 Открыть мобило на `http://localhost:8000`
- 🔍 Проверить API на `http://localhost:8000/docs` (Swagger)
- 📊 Похдакать рейсы, аэропорты, билеты

Если новое бывает сложным - напиши мне в Discord 😂
